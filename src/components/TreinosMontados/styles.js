import { StyleSheet } from "react-native";

export default styles = StyleSheet.create({
  container: {
    flex: 1,
    width: '100%',
    backgroundColor: '#FFFFFF',
    alignItems: 'center'
  },

  titulo: {
    margin: 20,
    fontSize: 16,
    fontWeight: '600'
  },

  lista: {
    width: '100%'
  }
});